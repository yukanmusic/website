git status
git add .
git commit -m"yukan git test"
git commit -m"yukan git test2"
git push


git remote -v

git branch -M main
git push -u origin main
git pull origin main --allow-unrelated-histories
:wq

git submodule deinit -f yukan-front
git rm -f yukan-front
rm -rf .git/modules/yukan-front
Remove-Item -Recurse -Force .git\modules\yukan-front

git rm --cached yukan-front
git status
git commit -m "Remove broken yukan-front submodule"
git push









https://github.com/yukanmusic/website